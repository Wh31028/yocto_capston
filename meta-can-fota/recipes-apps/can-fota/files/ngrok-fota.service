[Unit]
Description=ngrok Automatic Tunneling Service for CAN-FOTA
After=can-fota.service
Wants=can-fota.service

[Service]
Type=simple
ExecStart=/bin/sh -c 'sleep 5 && /usr/bin/ngrok http --config /etc/ngrok.yml 8000'
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
