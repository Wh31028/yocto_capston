# We have a conf and classes directory, add to BBPATH
BBPATH .= ":${LAYERDIR}"

# We have recipes-* directories, add to BBFILES
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "meta-can-fota"
BBFILE_PATTERN_meta-can-fota = "^${LAYERDIR}/"
BBFILE_PRIORITY_meta-can-fota = "6"

LAYERDEPENDS_meta-can-fota = "core"
LAYERSERIES_COMPAT_meta-can-fota = "scarthgap"
